package com.thebigfun.booking_service.repository;import com.thebigfun.booking_service.model.entity.Booking;import org.springframework.data.jpa.repository.JpaRepository;import org.springframework.stereotype.Repository;import java.time.LocalDateTime;import java.util.List;@Repositorypublic interface Booking_ServiceRepository extends JpaRepository<Booking, Long > {// Aquí puedes definir métodos personalizados que necesites para consultar la base de datos    //  Encontrar todas las reservas por el ID del usuario    List<Booking> findByUserId(Long  userId);    // Encontrar todas las reservas por el estado de la reserva    List<Booking> findByStatus(Booking.BookingStatus status);    // Método para encontrar todas las reservas por estado    List<Booking> findAllByStatus(Booking.BookingStatus status);    // Método para encontrar reservas por estado y que tienen una fecha de expiración antes de cierto tiempo    List<Booking> findAllByStatusAndExpirationDateBefore(Booking.BookingStatus status, LocalDateTime dateTime);}