package com.thebigfun.ticket_service.repository;import com.thebigfun.ticket_service.model.Ticket;import org.springframework.data.jpa.repository.JpaRepository;import org.springframework.stereotype.Repository;import reactor.core.publisher.Flux;import java.util.List;@Repositorypublic interface TicketRepository extends JpaRepository<Ticket, Long > { // este metodo nos recuperar tickets por userId y el estado isActive    //Flux<Ticket> findByUserIdAndIsActive(Long userId, Boolean isActive);    List<Ticket> findByUserIdAndIsActive(Long userId, Boolean isActive);}