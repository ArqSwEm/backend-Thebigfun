package com.thebigfun.ticket_service.model;import jakarta.persistence.*;import lombok.AllArgsConstructor;import lombok.Data;import lombok.NoArgsConstructor;import java.time.LocalDateTime;@Data@AllArgsConstructor@NoArgsConstructor@Entity@Table(name = "ticket_services")public class Ticket {    @Id    @GeneratedValue(strategy = GenerationType.IDENTITY)    private Long id;    private String category; // La categoría del asiento (VIP, General, etc.)    private Double price; // El precio del ticket    @Enumerated(EnumType.STRING)    private TicketStatus status; // El estado del ticket (disponible, reservado, vendido)    private String qrCode; // Código QR asociado al ticket para su validación    @Column(name = "event_id")    private Long eventId; // El ID del evento asociado al ticket    @Column(name = "user_id")    private Long userId; // El ID del usuario que adquirió el ticket    private Boolean isActive = true; // Indica si el ticket está activo    private LocalDateTime deletedAt; // Fecha y hora de la "eliminación"    public enum TicketStatus {        AVAILABLE,        RESERVED,        SOLD    }}